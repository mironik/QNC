[package]
name = "qnc-editorial-shell"
version = "0.1.0"
edition = "2021"
description = "QNC public passive module: editorial shell (left media column, divider, right panel) and monitor preview"

[dependencies]
eframe = { workspace = true }

[dev-dependencies]
qnc-media-card = { workspace = true }
qnc-media-pool-head = { workspace = true }
serde_json = { workspace = true }
