[package]
name = "qnc-media-pool-head"
version = "0.1.0"
edition = "2021"
description = "QNC public passive module: media pool head (library tabs and compact transport strip)"

[dependencies]
eframe = { workspace = true }
