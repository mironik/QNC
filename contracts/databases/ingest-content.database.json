{
  "database_id": "qnc.db.ingest_content",
  "owner_application": "qnc.ingest",
  "schema_version": "0.2.0",
  "qnc_uri": "qnc://local/db/ingest_content",
  "transport_uri_templates": [
    "qnc://local/db/ingest_content/{project_id}",
    "qnc://lan/{authority}/db/ingest_content/{project_id}",
    "qnc://intranet/{authority}/db/ingest_content/{project_id}"
  ],
  "transport_scope": ["local", "lan", "intranet"],
  "tables": [
    "clips",
    "clip_sources",
    "clip_proxy",
    "probe_records",
    "filmstrip_artifacts",
    "filmstrip_frames",
    "wave_artifacts"
  ],
  "public_read_views": [
    "public_clips",
    "public_clip_sources",
    "public_clip_proxy",
    "public_probe_records",
    "public_filmstrip_artifacts",
    "public_filmstrip_frames",
    "public_wave_artifacts"
  ],
  "write_owner": "qnc.ingest",
  "public_read_policy": "public_read_views",
  "project_scope": "The existing workspace DB URI resolves through its owner-provided binding. Ingest owns only its tables inside that project database; Project settings, registry, identity and activation are never writable through this adapter. No fallback project or second global clip catalog.",
  "implementation_private_files": ["{registered_project_directory}/qnc_project.db"],
  "implementation_status": "Select publication, persisted selection and startup/reload are connected through the public content adapter. Filmstrip artifact publication stores one summary row and one public URI row per generated frame. Import execution is not implemented in this step.",
  "storage_policy": "Validate project identity before writes. Preserve existing WAL mode; otherwise use PERSIST rollback journals with short transactions and busy_timeout. Restore only required owner write permissions on validated output DB/journal directory, never remove delete ACLs or touch source card permissions.",
  "public_record_policy": "Accept a persisted metadata revision without running a new probe. Selection and import state are durable; media_records retains acquisition evidence.",
  "transport_endpoint": "/v1/ingest-content",
  "transport_contract_version": "0.2.4",
  "single_clip_read": "read(clip_id) returns one stored catalog/probe snapshot or null. Read-only, same Local/LAN/Intranet endpoint and identity validation. Schema remains 0.2.0; no migration or data rewrite. Peers must use the same transport contract version.",
  "catalog_summary_read": "list_summary returns paged lightweight catalog rows from materialized clip/source columns for UI startup. It must not deserialize catalog_json, probe_json, thumbnails or media snapshots. Full clip/probe payload is read only through read(clip_id) when a module actually needs it.",
  "catalog_stats_read": "stats returns only a lightweight catalog signature: clip_count, selected_count, revision_sum, max_revision and fingerprint. App activation may use it to decide whether the already displayed catalog is stale, but it must not replace Select and must not scan cards or run probe.",
  "select_reconciliation": {
    "inventory": "Paged clip_id/source_uri/original_uri/revision/final facts, without probe JSON",
    "publish_batch": "At most 16 records in one transaction; previews may precede commit, success may not",
    "remove_missing": "Explicit confirmed source absence only, scoped by identity and revision; retain queued, processing and imported records; never delete media or acquisition evidence",
    "existing_marker": "Transient view projection of membership in the DB before the current Select, not import status"
  },
  "record_contracts": {
    "public_probe_records.probe_json": {
      "contract_id": "qnc.media.metadata",
      "contract_version": "0.2.0",
      "module_id": "qnc.module.media-metadata",
      "specification": "docs/25-media-metadata-contract.md"
    },
    "public_filmstrip_frames.artifact_uri": {
      "contract_id": "qnc.filmstrip.frame",
      "contract_version": "0.1.0",
      "module_id": "qnc.module.filmstrip",
      "specification": "13 real JPEG frame artifacts in the project filmstrip directory; no probe, no scanner, no repeated poster."
    },
    "public_wave_artifacts.peaks_json": {
      "contract_id": "qnc.wave.peaks",
      "contract_version": "0.1.0",
      "module_id": "qnc.module.wave",
      "specification": "Prepared peak arrays for passive timeline painting. Source and lane count come from project audio settings plus saved clip/proxy metadata: two-channel project may use proxy audio when present, original uses saved original channels in public timeline lanes. No waveform probe, no PNG sidecar, no separate wave database."
    }
  },
  "migration_policy": "owner_application_only",
  "created_at": "2026-09-04T00:00:00Z",
  "updated_at": "2026-09-11T00:00:00Z"
}
